export enum ScreenSize {
    MOBILE = "336px",
    TABLET = "768px",
    DESKTOP = "1440px",
}

export enum AccountType {
    STUDENT = "student",
    ADMIN = "admin",
    FACULTY = "faculty",
}